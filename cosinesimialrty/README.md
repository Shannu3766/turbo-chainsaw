# adalora_bi_eq13

This package mirrors adalora_bi but replaces the BI importance computation with Equation (1.3):
gradient × activation (absolute).

Usage:
    pip install -e .
    python -m examples.finetune_eq13_demo
